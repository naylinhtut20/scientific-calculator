# Scientific Calculator

A PyQt5-based scientific calculator with:

- AST-based safe expression evaluation
- History tracking
- Square root, factorial, constants (π, e)
- Smart parentheses handling
- Error handling

## Installation

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python main.py
